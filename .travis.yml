dist: trusty
sudo: false
language: python
python:
  - "3.6"
  - "nightly"
compiler:
  - gcc
  - clang
install:
  - pip install cython
  - pip install tox
before_script:
  - make cython
  - pip install grpcio-tools
  - make protos
script:
  - travis_wait 60 tox

============
Installation
============

At the command line::

    $ python3 -m pip install httpstan

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv httpstan
    $ python3 -m pip install httpstan
